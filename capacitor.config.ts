import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ph.u1.cristhianbravo',
  appName: 'ph-u1-cristhianbravo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
